---
title: Selecting items
order: 5
layout: page
---

[[datamodel.selection]]
= Selecting items

////
TODO
Outline
* Intro: Some components are primarily for selection (HasValue, single or multi), some not
* primarily selection
 * comboBox.setItems(EnumSet.allOf(Status.class));
 * value == selection -> directly usable with Binder
 * value change event = selection change event
 * Special event with added / removed for multi select
* not HasValue
 * selection models
 * events through models
 * asSingle, asMulti for using with Binder
////
